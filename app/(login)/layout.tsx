export default function LoginLayout({children}: {children: React.ReactNode}) {
    return <section className="flex flex-col justify-center items-center gap-10">
            {children}
        </section>
  }